version: "3"

services:
  speedtest:
    build:
        context: .
        dockerfile: Dockerfile
    restart: unless-stopped
    environment:
        - DISCORD_WEBHOOK=${DISCORD_WEBHOOK}
        - TIMEZONE=${TIMEZONE}
